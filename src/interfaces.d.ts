export interface Surface {
	/**
	 *
	 */
/*
	 Circle(): Shape
*/
	/**
	 * Creates a surface.
	 */
	createSurface(parentNode: HTMLElement, width: number, height: number): Surface;
/*
    decompose(matrix) Decompose a 2D matrix into translation, scaling, and rotation components.
	Ellipse() 
	equalSources(a,b) compares event sources, returns true if they are equal
	formatNumber(x,addSpace) converts a number to a string using a fixed notation
	getVectorFont(url) 
	Group() 
	Line() 
	makeFontString(font) converts a font object to a CSS font string
	makeParameters(defaults,update) copies the original object, and all copied properties from the 'update' object
	Matrix2D(arg) a 2D matrix object
	Moveable() 
	Mover() 
	normalizeColor(color) converts any legal color representation to normalized dojo/Color object
	normalizedLength(len) converts any length value to pixels
	normalizeParameters(existed,update) updates an existing object with properties from an 'update' object
	Path() 
	Point() 
	Polyline() 
	pt2px(len) converts points to pixels
	px2pt(len) converts pixels to points
	px_in_pt() returns the current number of pixels per point.
	Rect() 
	Rectangle() 
	splitFontString(str) converts a CSS font string to a font object
	Surface() 
	switchTo(renderer) switch the graphics implementation to the specified renderer.
	Text() 
	TextPath() 
	VectorFont() 
	*/
}
